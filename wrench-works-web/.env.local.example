# Backend API URL (server-side only — never exposed to browser)
API_BASE_URL=http://localhost:5000

# Cookie encryption secret (generate a random 32+ char string)
SESSION_SECRET=change-me-to-a-random-secret-at-least-32-chars
